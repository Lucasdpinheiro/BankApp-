package mybank.models;

public class NaturalPersonClient extends Client {

    protected NaturalPersonClient(String name, String email, String password) {
        super(name, email, password);
    }
}
